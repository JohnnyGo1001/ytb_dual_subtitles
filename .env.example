# YouTube 双语字幕系统环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# ==================== 存储路径配置 ====================

# 视频文件存储路径（默认: ~/ytb/videos）
# YTB_DOWNLOAD_PATH=~/ytb/videos

# 字幕文件存储路径（默认: ~/ytb/subtitles）
# YTB_SUBTITLE_PATH=~/ytb/subtitles

# 应用数据存储路径，包括数据库（默认: ~/ytb/data）
# YTB_DATA_PATH=~/ytb/data

# ==================== 数据库配置 ====================

# 数据库 URL（默认: sqlite+aiosqlite:///{YTB_DATA_PATH}/ytb.db）
# YTB_DATABASE_URL=sqlite+aiosqlite:///~/ytb/data/ytb.db

# 生产环境可以使用 PostgreSQL:
# YTB_DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/ytb_db

# ==================== 浏览器 Cookie 配置 ====================

# 浏览器名称（默认: chrome）
# 支持的浏览器: chrome, firefox, safari, edge
# YTB_BROWSER_NAME=chrome

# 浏览器配置文件名称（推荐配置！）
# 如果不指定，系统会尝试使用默认配置文件
#
# 如何获取配置文件名称：
# 1. 打开 Chrome 浏览器
# 2. 在地址栏输入: chrome://version/
# 3. 查看 "个人资料路径" (Profile Path)
#    例如: /Users/johnny/Library/Application Support/Google/Chrome/Default
#    最后一段 "Default" 就是配置文件名称
#
# 常见的配置文件名称:
# - Default (默认配置文件)
# - Profile 1 (第二个配置文件)
# - Profile 2 (第三个配置文件)
#
# 示例:
YTB_BROWSER_PROFILE=Default

# ==================== 下载配置 ====================

# 最大并发下载任务数（默认: 3，范围: 1-10）
# YTB_MAX_CONCURRENT_DOWNLOADS=3

# 下载超时时间（秒，默认: 300）
# YTB_DOWNLOAD_TIMEOUT=300

# 下载失败重试次数（默认: 2，范围: 0-5）
# 重试策略：首次尝试失败后，会等待2秒、4秒后分别重试
# YTB_DOWNLOAD_MAX_RETRIES=2

# ==================== yt-dlp 下载配置 ====================

# 自定义视频格式选择（留空使用智能默认选择）
# 默认策略会自动选择最佳视频+音频格式并合并
#
# 常用格式示例：
#   最高 1080p: bestvideo[height<=1080]+bestaudio/best[height<=1080]
#   最高 720p:  bestvideo[height<=720]+bestaudio/best[height<=720]
#   最高 480p:  bestvideo[height<=480]+bestaudio/best[height<=480]
#   仅音频:    bestaudio/best
#   特定编码:  bestvideo[vcodec^=avc]+bestaudio[acodec^=mp4a]/best
#
# YTB_YT_DLP_FORMAT=

# 是否下载字幕（默认: true）
# YTB_YT_DLP_WRITE_SUBTITLES=true

# 是否下载自动生成的字幕（默认: true）
# YTB_YT_DLP_WRITE_AUTO_SUBTITLES=true

# 要下载的字幕语言（逗号分隔，默认: en,zh-CN,zh-Hans,zh-Hant,zh-TW,ja,ko,es,fr,de）
# YTB_YT_DLP_SUBTITLE_LANGUAGES=en,zh-CN,zh-Hans,zh-Hant,zh-TW,ja,ko,es,fr,de

# 字幕格式（默认: vtt，可选: vtt, srt, ass, lrc）
# YTB_YT_DLP_SUBTITLE_FORMAT=vtt

# 合并后的输出格式（默认: mp4，可选: mp4, mkv, webm, avi）
# YTB_YT_DLP_MERGE_OUTPUT_FORMAT=mp4

# ==================== Web 服务器配置 ====================

# API 服务监听地址（默认: 127.0.0.1）
# YTB_HOST=127.0.0.1

# API 服务监听端口（默认: 8000，范围: 1024-65535）
# YTB_PORT=8000

# 前端 URL（用于 CORS 配置，默认: http://localhost:3000）
# YTB_FRONTEND_URL=http://localhost:3000

# ==================== 应用配置 ====================

# 调试模式（默认: false）
# YTB_DEBUG=false

# 日志级别（默认: INFO，可选: DEBUG, INFO, WARNING, ERROR）
# YTB_LOG_LEVEL=INFO

# ==================== 常用配置示例 ====================

# 场景 1: 使用第二个 Chrome 配置文件
# YTB_BROWSER_PROFILE=Profile 1

# 场景 2: 只下载 720p 视频以节省空间和带宽
# YTB_YT_DLP_FORMAT=bestvideo[height<=720]+bestaudio/best[height<=720]

# 场景 3: 只下载英文和中文字幕
# YTB_YT_DLP_SUBTITLE_LANGUAGES=en,zh-CN,zh-Hans

# 场景 4: 使用自定义存储路径
# YTB_DOWNLOAD_PATH=/Users/yourname/Videos/YouTube
# YTB_SUBTITLE_PATH=/Users/yourname/Videos/YouTube/Subtitles
# YTB_DATA_PATH=/Users/yourname/Videos/YouTube/Data

# 场景 5: 增加并发下载数（需要更好的网络和带宽）
# YTB_MAX_CONCURRENT_DOWNLOADS=5

# 场景 6: 使用 Firefox 浏览器的 Cookie
# YTB_BROWSER_NAME=firefox
# YTB_BROWSER_PROFILE=default-release

# 场景 7: 只下载音频（制作播客或音乐）
# YTB_YT_DLP_FORMAT=bestaudio/best
# YTB_YT_DLP_MERGE_OUTPUT_FORMAT=mp3

# 场景 8: 下载 SRT 格式字幕（兼容性更好）
# YTB_YT_DLP_SUBTITLE_FORMAT=srt

# ==================== 说明 ====================

# 1. 所有路径支持 ~ 表示用户主目录
# 2. 如果不设置某个选项，将使用默认值
# 3. 修改配置后需要重启应用才能生效
# 4. .env 文件不会被提交到 Git，请妥善保管敏感信息
# 5. 带 YTB_ 前缀的环境变量会被系统自动识别
# 6. 注释行（以 # 开头）不会被读取
