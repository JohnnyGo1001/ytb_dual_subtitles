# YouTube 双语字幕系统配置文件
# 此文件用于配置应用的各项参数
# 修改后需要重启应用才能生效

# ==================== 浏览器 Cookie 配置 ====================
# 用于从浏览器提取 YouTube 登录 Cookie

# 浏览器名称（chrome, firefox, safari, edge）
YTB_BROWSER_NAME=chrome

# 浏览器配置文件名称（推荐配置）
# 获取方式：在 Chrome 地址栏输入 chrome://version/
# 查看 "个人资料路径"，最后一段就是配置文件名
# 例如：Default, Profile 1, Profile 2
YTB_BROWSER_PROFILE=Default

# ==================== 数据库配置 ====================

# 数据库连接 URL（默认使用 SQLite）
YTB_DATABASE_URL=sqlite+aiosqlite:///~/ytb/data/ytb.db

# ==================== 存储路径配置 ====================

# 视频文件存储路径
YTB_DOWNLOAD_PATH=~/ytb/videos

# 字幕文件存储路径
YTB_SUBTITLE_PATH=~/ytb/subtitles

# 应用数据存储路径（包括数据库）
YTB_DATA_PATH=~/ytb/data

# ==================== 下载配置 ====================

# 最大并发下载任务数（1-10，默认: 3）
YTB_MAX_CONCURRENT_DOWNLOADS=3

# 下载超时时间（秒，默认: 300）
YTB_DOWNLOAD_TIMEOUT=300

# 下载失败重试次数（0-5，默认: 2）
YTB_DOWNLOAD_MAX_RETRIES=2

# ==================== yt-dlp 视频下载配置 ====================

# 视频格式选择（留空使用智能默认选择）
# 常用示例：
#   最高 1080p: bestvideo[height<=1080]+bestaudio/best[height<=1080]
#   最高 720p:  bestvideo[height<=720]+bestaudio/best[height<=720]
#   仅音频:    bestaudio/best
# 如果需要限制质量，取消下面的注释并设置
# YTB_YT_DLP_FORMAT=bestvideo[height<=1080]+bestaudio/best[height<=1080]

# 是否下载字幕（默认: true）
YTB_YT_DLP_WRITE_SUBTITLES=true

# 是否下载自动生成的字幕（默认: true）
YTB_YT_DLP_WRITE_AUTO_SUBTITLES=true

# 要下载的字幕语言（逗号分隔）
YTB_YT_DLP_SUBTITLE_LANGUAGES=en,zh-CN

# 字幕格式（默认: vtt，可选: vtt, srt, ass）
YTB_YT_DLP_SUBTITLE_FORMAT=vtt

# 合并后的输出格式（默认: mp4，可选: mp4, mkv, webm）
YTB_YT_DLP_MERGE_OUTPUT_FORMAT=mp4

# ==================== Web 服务器配置 ====================

# 服务器监听地址（默认: 127.0.0.1）
YTB_HOST=127.0.0.1

# 服务器监听端口（默认: 8000）
YTB_PORT=8000

# 前端 URL（用于 CORS 配置，默认: http://localhost:3000）
YTB_FRONTEND_URL=http://localhost:3000

# ==================== 应用配置 ====================

# 调试模式（默认: false）
YTB_DEBUG=false

# 日志级别（默认: INFO，可选: DEBUG, INFO, WARNING, ERROR）
YTB_LOG_LEVEL=INFO

# ==================== 说明 ====================
# 1. 以 # 开头的行是注释，不会被读取
# 2. 所有路径支持 ~ 表示用户主目录
# 3. 修改配置后需要重启应用
# 4. 更多配置选项和示例请参考 .env.example 文件
